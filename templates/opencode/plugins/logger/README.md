# OpenCode Logger Plugin

TypeScript plugin for automated logging in OpenCode (equivalent to Claude Code hooks.json).

## Installation

```bash
cp -r logger .opencode/plugins/
echo '{"plugins": ["./plugins/logger"]}' > .opencode/opencode.json
```

## Documentation

See `references/opencode_logging_protocol.md` for complete usage instructions.
