# Hosts

| Host | Path |
|---|---|
| Universal | `plugin.json` (agent-plugins.org). Skill lives at repo-root `SKILL.md`. |
| Claude Code | `.claude-plugin/` |
| Grok Build | `.grok-plugin/` (Claude-compatible, zero-config) |
| Codex | `.codex-plugin/` |
| Cursor | `.cursor-plugin/` + `.cursor/rules/` |

No Claude `hooks/hooks.json`, so Codex/Cursor hook files are not required.

Catalog: SpillwaveSolutions/skills-marketplace (`spillwave-skills`).
